# 🚀 Быстрый старт: Использование BaseID в smux

## ⚡ Самое важное

```go
// НОВОЕ: Создайте стрим с базовым ID
stream, err := session.OpenStreamWithBaseID(42)

// НОВОЕ: Получите базовый ID на другой стороне
baseID := stream.BaseID()  // → 42
```

## 📖 Примеры кода

### Пример 1: Привязка ID запроса к стриму

```go
// Клиент: Отправить запрос с ID 42
requestID := uint32(42)
stream, _ := clientSession.OpenStreamWithBaseID(requestID)
defer stream.Close()

stream.Write([]byte("my request data"))
// ...

// Сервер: Получить запрос с его ID
stream, _ := serverSession.AcceptStream()
defer stream.Close()

requestID := stream.BaseID()
log.Printf("Received request #%d\n", requestID)

buf := make([]byte, 1024)
n, _ := stream.Read(buf)
// Process request with ID
```

### Пример 2: Маршрутизация по типам сессий

```go
const (
    TypeVideo   uint32 = 100
    TypeAudio   uint32 = 200
    TypeControl uint32 = 300
)

// Клиент
videoStream, _ := session.OpenStreamWithBaseID(TypeVideo)
audioStream, _ := session.OpenStreamWithBaseID(TypeAudio)
controlStream, _ := session.OpenStreamWithBaseID(TypeControl)

// Сервер
for {
    stream, _ := session.AcceptStream()
    
    switch stream.BaseID() {
    case TypeVideo:
        go handleVideoStream(stream)
    case TypeAudio:
        go handleAudioStream(stream)
    case TypeControl:
        go handleControlStream(stream)
    }
}
```

### Пример 3: Отслеживание сессий

```go
// Клиент: Создать сессию с ID
sessionID := uint32(time.Now().Unix())

for i := 0; i < 10; i++ {
    stream, _ := session.OpenStreamWithBaseID(sessionID)
    // Используем stream
    // Все потоки этой сессии имеют одинаковый baseID
    stream.Close()
}

// Сервер: Группировать потоки по сессиям
sessions := make(map[uint32][]*Stream)

for {
    stream, _ := session.AcceptStream()
    sessionID := stream.BaseID()
    sessions[sessionID] = append(sessions[sessionID], stream)
    // Обработать множество потоков одной сессии
}
```

## ✅ Что изменилось?

| Что | Было | Теперь |
|-----|------|--------|
| Создание стрима | `OpenStream()` | `OpenStream()` или `OpenStreamWithBaseID(id)` |
| Получение ID | `stream.ID()` | `stream.ID()` + `stream.BaseID()` |
| Обратная совместимость | ✅ | ✅ (100% совместима) |

## 🔍 Часто задаваемые вопросы

### Q: Обязательно ли использовать OpenStreamWithBaseID?
**A:** Нет, можно продолжать использовать обычный `OpenStream()`. Для него `baseID` будет 0.

### Q: Что если я отправлю baseID=0?
**A:** Это то же самое, что использовать `OpenStream()`. В payload SYN фрейма ничего не будет отправляться.

### Q: Можно ли получить ID стрима отдельно?
**A:** Да, используйте `stream.ID()` для получения внутреннего ID, и `stream.BaseID()` для базового ID.

### Q: Какой размер имеет baseID?
**A:** 32-битное беззнаковое целое число (uint32), от 0 до 4,294,967,295.

### Q: Сколько данных добавляется в сеть?
**A:** Всего 4 байта в payload SYN фрейма (только если baseID != 0).

### Q: Совместимо ли с существующим кодом?
**A:** Да, 100% совместимо. Старый код работает без изменений.

## 📚 Дополнительная информация

- Подробная документация: [BASEID_IMPLEMENTATION.md](./BASEID_IMPLEMENTATION.md)
- Полное резюме: [BASEID_CHANGES_SUMMARY.md](./BASEID_CHANGES_SUMMARY.md)
- Примеры использования: [example_baseid_test.go](./example_baseid_test.go)
- Юнит-тесты: [baseid_test.go](./baseid_test.go)

## 🎯 Типичные use cases

1. **Отслеживание запросов**: Привяжите ID запроса к стриму для отслеживания
2. **Маршрутизация**: Используйте baseID для маршрутизации стримов по типам
3. **Идентификация клиентов**: Привяжите ID клиента к потокам
4. **Группировка логических сессий**: Группируйте связанные потоки с одинаковым baseID
5. **Отладка**: Используйте baseID для логирования и отладки

## 🚀 Готово к использованию!

Функциональность полностью реализована, протестирована и готова к использованию в боевых условиях.

---

**Версия**: 1.0  
**Дата**: 27 декабря 2025  
**Статус**: ✅ Готово
